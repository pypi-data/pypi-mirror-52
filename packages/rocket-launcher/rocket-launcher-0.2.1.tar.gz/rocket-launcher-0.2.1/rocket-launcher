#!/usr/bin/env python
import os
import threading
import time
from rockets import dockerd
w = threading.Thread(target=dockerd, name='docker')
w.start()
time.sleep(3)
os.system('sudo -H -u who byobu');
