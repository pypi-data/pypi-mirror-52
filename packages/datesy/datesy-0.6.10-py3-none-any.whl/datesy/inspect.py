def find_header_line(data, header_key):
    """
    Find the header line in row_based data_structure

    Parameters
    ----------
    data : rows?!
    header_key
        some key to find in row ? regex of "".join(header_row)?

    Returns
    -------

    """
    raise NotImplemented


def find_key(data, key):
    """
    Find a key in a complex dictionary

    Parameters
    ----------
    data : dict
        the handling
    key : str
        (pattern) to be found

    Returns
    -------

    """
    # return "path in structure", "value"
    raise NotImplemented
