from setuptools import setup

setup(name='distributions_metric',
      version='1.0',
      description='Gaussian and Binomial distributions',
      packages=['distributions_metric'],
      author='Yu Zheng',
      zip_safe=False)
