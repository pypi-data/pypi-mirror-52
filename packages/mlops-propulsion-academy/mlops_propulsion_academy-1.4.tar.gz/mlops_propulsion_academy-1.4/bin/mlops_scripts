#!/usr/bin/env bash

if [ $1 == "install" ]
then
    echo "installing"
    install.sh
elif [ $1 == "update" ]
then
   echo "updating"
   update.sh
elif [ $1 == "start" ]
then
    echo "starting"
    start.sh
fi
