from setuptools import setup

setup(name='udacity_gaussian_binomial_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['udacity_gaussian_binomial_distributions'],
      zip_safe=False)
