from setuptools import setup

setup(name='rt_probability',
      version='1.0',
      description='Gaussian and Binomial distributions',
      packages=['rt_probability'],
      author='Ravi T.',
      zip_safe=False)
